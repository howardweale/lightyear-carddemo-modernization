![LIGHTYEAR primary logo](../../../brand/assets/lightyear-primary.svg)

*Where context becomes trusted action.*

# MS #54 - CloudBank Executable Source Baseline

> **Status:** Complete
>
> **Release:** 0.54.0
>
> **Date:** 2026-09-02
>
> **Audience:** Customers, partners, executives, architects, delivery teams, and assurance reviewers

## Executive summary

MS #54 - CloudBank Executable Source Baseline converts a specific part of the LIGHTYEAR modernization approach into a governed, reviewable capability. Turns the pinned CloudBank estate into a reproducible, fail-closed source execution contract so the first dark factory workcell can start from proven Oracle behavior instead of static inventory alone.

## Why this matters to a customer

Turns the pinned CloudBank estate into a reproducible, fail-closed source execution contract so the first dark factory workcell can start from proven Oracle behavior instead of static inventory alone.

The practical result is a narrower, evidence-backed decision instead of a broad modernization claim. The milestone packages capability, proof, and limitations together so commercial, architecture, delivery, security, and assurance teams can review the same record.

## What the milestone delivers

- A deterministic Java 21 and Maven 3.6-or-newer build plan bound to the exact 189-file, 70-Java-unit, ten-module CloudBank v5 source tree.
- A reproducible build of the seven-service set declared by the pinned upstream image-build script, with seven JAR hashes required in the signed receipt.
- A bounded Oracle runtime plan using the pinned azn-server Testcontainers suite: three integration classes, seven tests, and gvenzl/oracle-free:23.26.1-slim-faststart.
- Content-addressed readiness, build, and Oracle-runtime contracts plus three JSON Schemas.
- Environment-key-signed receipts that retain aggregate results and hashes while excluding credentials, raw stdout, and raw stderr.
- Strict verification rejecting unsigned evidence, source or plan drift, Java 17, old Maven, missing artifacts, failed or skipped tests, image-identity gaps, and PostgreSQL or production overclaims.
- Cross-platform launchers and a Java 21 CI source-build job that exercises the exact pinned upstream checkout.
- An explicit data boundary: pinned sample and bootstrap data qualify the first controlled run, while production data requires a separately authorized and profiled extract.

## How it differs from earlier work

MS #53 selected, pinned, inventoried, and projected CloudBank without building or running it. MS #54 defines the exact full-source build and bounded native Oracle evidence required before PostgreSQL mapping or an autonomous customer-service transformation may begin.

Earlier work remains useful evidence, but it is not automatically promoted into this milestone. A successful parser, simulator, local comparison, or generated artifact is not presented as live platform equivalence or production readiness.

## What a customer can do with it

A customer can use cloudbank executable source baseline as a bounded decision and delivery capability. It can be attached to the customer's approved source package, owners, policies, target architecture, acceptance criteria, and authorized evidence.

Unresolved semantic loss, policy decisions, unsupported behavior, and live-evidence requirements remain visible rather than being averaged into one pass/fail statement.

## Evidence and acceptance posture

The milestone is complete as a repository capability because its committed implementation and release record are present. Customer qualification remains claim-specific: only the delivered behaviors listed above are supported, and only at the evidence class recorded by their underlying receipts and gates.

A customer review should confirm:

1. the exact source, graph, policy, fixture, and artifact identities referenced by the evidence;
2. the difference between deterministic development evidence and authorized live evidence;
3. every policy decision, lossy transformation, unsupported behavior, and explicit exclusion;
4. the acceptance gates that passed and the live or production gates that remain blocked; and
5. that later changes have not invalidated the content-addressed evidence.

## Boundaries and claims not made

- The committed readiness receipt says ready-to-execute-not-observed because this development environment has not produced an authorized Java 21, Docker, and Oracle execution receipt.
- The bounded Oracle runtime suite proves only the azn-server source baseline; it does not execute the customer service or the complete CloudBank application.
- The upstream source is required as an exact external checkout and is not vendored into this repository.
- No customer or production CloudBank data is present, copied, or implied.
- PostgreSQL target selection and mapping do not begin until the source runtime receipt is admitted.
- PostgreSQL compatibility, target equivalence, application equivalence, migration completion, and production readiness remain false.

These boundaries are part of the deliverable. They prevent bounded development evidence from becoming an unsupported customer promise.

## Source of record

- Release record: [CHANGELOG.md](https://github.com/howardweale/lightyear-carddemo-modernization/blob/main/CHANGELOG.md), release section(s) 0.54.0.
- Product and operating guidance: [README.md](https://github.com/howardweale/lightyear-carddemo-modernization/blob/main/README.md).
- Governing sequence and claim classifications: [LIGHTYEAR-ROADMAP.md](https://github.com/howardweale/lightyear-carddemo-modernization/blob/main/LIGHTYEAR-ROADMAP.md).
- Machine-readable milestone metadata: [catalog.json](https://github.com/howardweale/lightyear-carddemo-modernization/blob/main/docs/milestones/catalog.json).
- Artifact integrity: [manifest.json](https://github.com/howardweale/lightyear-carddemo-modernization/blob/main/docs/milestones/manifest.json).

---

This document is a customer-readable explanation of committed repository evidence. The underlying schemas, receipts, ledgers, tests, and policy decisions remain authoritative when greater detail is required.
