<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <!-- The agent supplies trace_id and span_id from the active server span.
         Never render the DispatcherServlet message or throwable: either can
         contain request paths, query strings, headers or application data. -->
    <appender name="REQUEST_CONTEXT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>{"severity":"DEBUG","message":"cloudbank-request-context","event":"cloudbank-request-context","logging.googleapis.com/trace":"%replace(%X{trace_id}){'^([0-9a-f]{32})$','projects/{{PROJECT_ID}}/traces/$1'}","logging.googleapis.com/spanId":"%X{span_id}"}%nopex%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    <logger name="org.springframework.web.servlet.DispatcherServlet" level="DEBUG" additivity="false">
        <appender-ref ref="REQUEST_CONTEXT"/>
    </logger>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
